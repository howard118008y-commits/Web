# 圖片原檔備份（壓縮前）— 本地保留即可，不進 repo
img/_original/

# macOS
.DS_Store

# Editor
.vscode/
*.swp

# Claude Code local memory (project knowledge graph, not for sharing)
.claude/memory.json

# Claude Code session caches
.claude/cache/
.claude/sessions/
