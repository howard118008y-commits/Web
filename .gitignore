# 圖片原檔備份（壓縮前）— 本地保留即可，不進 repo
img/_original/

# macOS
.DS_Store

# Editor
.vscode/
*.swp

# Claude Code local memory (project knowledge graph, not for sharing)
.claude/memory.json

# Claude Code session caches
.claude/cache/
.claude/sessions/

# LVR observatory pipeline cache (zips, intermediate pickle/csv)
# Note: 產物 lvr-charts/ + lvr-data/ + lvr-observatory.html 會 commit；cache 不會
scripts/lvr/_cache/
scripts/lvr/__pycache__/

# iCloud Drive 衝突複本（專案夾在 iCloud，多機同步會生「名 2.ext」）＋Python 快取＋暫存預覽
# 詳見 memory feedback_icloud_conflict_copies
* 2.*
__pycache__/
_preview_*.html

# goal 評分迴圈輸出（機器產出流水，本地保留即可）
scripts/goal-scores.jsonl
