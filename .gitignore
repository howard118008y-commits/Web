# 圖片原檔備份（壓縮前）— 本地保留即可，不進 repo
img/_original/

# macOS
.DS_Store

# Editor
.vscode/
*.swp

# Claude Code local memory (project knowledge graph, not for sharing)
.claude/memory.json

# Claude Code session caches
.claude/cache/
.claude/sessions/

# LVR observatory pipeline cache (zips, intermediate pickle/csv)
# Note: 產物 lvr-charts/ + lvr-data/ + lvr-observatory.html 會 commit；cache 不會
scripts/lvr/_cache/
scripts/lvr/__pycache__/
